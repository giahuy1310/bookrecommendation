[pytest]
pythonpath = .
testpaths = tests
filterwarnings =
    # starlette 1.6 TestClient still imports httpx; httpx2 not yet pinned in this stack
    ignore:Using `httpx` with `starlette.testclient` is deprecated:starlette.exceptions.StarletteDeprecationWarning
